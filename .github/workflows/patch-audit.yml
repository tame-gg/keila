name: Patch Audit

on:
  pull_request:
  push:
    branches:
      - main
      - "keila/**"

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check patch names and unresolved markers
        run: bash scripts/checkPatches.sh
